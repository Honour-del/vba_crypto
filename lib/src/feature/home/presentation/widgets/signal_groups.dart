import 'package:flutter/material.dart';

class SignalGroup extends StatelessWidget {
  const SignalGroup({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
